a ajouter à la base de données  : 

CREATE TABLE messages (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(50),
    username VARCHAR(50),
    objet VARCHAR(100),
    message TEXT,
    date_sent DATETIME
);
